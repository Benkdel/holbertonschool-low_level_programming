#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry Point
 *
 * @argc: argument count
 * @argv: arguments arrays
 *
 * Return: result of multiplying two numbers
 *
 */
int main(int argc, char *argv)
{
	if (argc < 3)
	{
		printf("Error\n");
		exit(98);
	}

	/* if argv[1] and argv[2] are not entirerly of digits, exit... */

	/* finish later */

	return (0);
}
